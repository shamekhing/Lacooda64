# Tools

`lacooda-asm.cpp` builds as `lacooda-asm` with `LACOODA64_BUILD_TOOLS=ON`.
It validates native assembly and optionally writes a lossless text listing.

Dataset-specific conversion belongs here, outside the ISA, runtime and assembler.
A full converter for `data/effects.txt` has not yet been implemented.
